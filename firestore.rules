rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: This is a temporary rule for debugging.
    // It allows all users to read and write to all data.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
