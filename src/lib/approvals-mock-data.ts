import type { ApprovalRequest, ApprovalItem, RecurringItem, BudgetItem } from '@/types';

// This file is now only responsible for exporting types, which it gets from the new central types file.
// The actual mock data has been removed as it was not being used and could cause confusion.
export type { ApprovalRequest, ApprovalItem, RecurringItem, BudgetItem };
